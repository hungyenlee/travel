# 設計決策總表

這份文件整理「台灣散步筆記」第一版在規劃與實作過程中談定的決策，方便日後回顧。
用語定義見 [../CONTEXT.md](../CONTEXT.md)；較重大、需要理由的決策另有 ADR（見 [adr/](./adr/)）。

## 架構

1. **多頁式（MPA）**：三個實體 HTML 檔（`index.html`、`city.html`、`detail.html`），頁面間用網址參數傳遞。捨棄單頁式（SPA），因為對新手最直覺、也符合規劃書。
2. **英文 slug 當識別碼**：城市 `?city=tainan`、地點 `?id=chihkan-tower`。避免中文在網址被編碼成亂碼。城市另存「slug ↔ 中文名」對照。
3. **單一 `data.js`**：所有城市的地點放同一份清冊（`cities` + `places` 扁平陣列 + `taxonomy`），`place.city` 存 slug。維護時只需開一個檔。
4. **共用層 + 每頁一支 JS**：`shared.js` ＋ `home.js`／`city.js`／`detail.js`，取代單一 `script.js`。詳見 [ADR-0003](./adr/0003-shared-layer-per-page-scripts.md)。

## 分類與篩選

5. **篩選選項來自固定允許清單（taxonomy）**，非從資料自動推導；填了清單外用詞會顯示警告橫幅。詳見 [ADR-0001](./adr/0001-fixed-taxonomy-with-warning.md)。
6. **清單涵蓋四類**：行政區（每城市一份）、景點分類、料理分類、特色標籤。**招牌品項不納入清單、維持自由填寫**。
7. **篩選邏輯**：第一層（全部／景點／美食）單選；第二層（行政區／分類／標籤）**組內單選、組間 AND（交集）**。
8. **空結果**：顯示友善訊息「找不到符合條件的地點，試試調整篩選」+ 清除篩選按鈕；篩選列常駐「清除篩選」。
9. **清單外標籤的提示**：以**頁面橫幅**顯示，給維護者看的安全網（非給遊客）。

## 地圖

10. **Leaflet + OpenStreetMap**，免金鑰、免費、無帳單風險（付費服務如 Mapbox／Google 一律未使用）。
11. **視野只在首次渲染自動框住（auto-fit）一次**所有地點座標；之後篩選／固定都不自動縮放，改由「顯示全部」按鈕手動全覽。`cities` 不需存中心點／縮放。詳見 [ADR-0004](./adr/0004-pin-places-map-only.md)。
12. **標記**：景點藍點、美食橘點（顏色由 CSS 變數提供，與圖例一致）＋ 圖例。
13. **預覽卡互動**：桌機滑鼠移入圓點浮出預覽卡、可移到卡片按按鈕、點圓點進詳細頁；手機點圓點顯示底部固定卡。改為自製卡片、不用 Leaflet popup，詳見 [ADR-0002](./adr/0002-self-managed-map-preview.md)。
14. **地圖／列表／摘要同步**：套用篩選時三者一起重繪。例外：**固定地點只疊加在地圖上**，列表與摘要仍只等於篩選結果，見 #21。
21. **固定地點**：可跨類型固定多個地點，讓它在切換篩選後仍留在地圖上（看某景點附近有哪些美食）。只影響地圖（篩選結果 ∪ 已固定、紫色粗外環）；列表與摘要維持誠實，僅把本來就在列表裡的固定卡片排到最前、加紫色粗框。只存記憶體、重整即清空。詳見 [ADR-0004](./adr/0004-pin-places-map-only.md)。

## 圖片與連結

15. **圖片**：真圖與佔位圖混用；缺圖或檔案不存在時自動退回 `assets/images/placeholder.svg`；卡片圖統一裁成 4:3。
16. **詳細頁大圖可點 → 開 Google 地圖（新分頁）**；列表卡、預覽卡的圖維持「進詳細頁」。`mapUrl` 直接存維護者提供的 Google 地圖連結。

## 頁面與內容

17. **首頁精選隨機**：每次載入從台南隨機抽景點 3、店家 3，不在資料裡標記 featured。
18. **導覽只留「台灣散步筆記（首頁）＋探索城市」**，拿掉全站層級的「景點／美食」（第一版沒有跨城市總表）。
19. **其他城市（高雄／台中／台北／花蓮）標「即將新增」、不可點**；把該城市 `available` 改為 `true` 並補地點即可開放。
20. **詳細頁版型**：桌機左圖右資訊、手機由上而下；景點與店家共用同一頁，僅少數欄位（招牌品項、開放／營業時間標籤）不同。

## 內容現況（第一版）

- 台南：景點 3（赤崁樓、四草綠色隧道、奇美博物館）+ 美食 4（文章牛肉湯、阿財牛肉湯、小方舟串燒酒場、赤崁中藥行）。
- 新增「赤崁中藥行」時，把「**酒吧**」設為**料理分類**（而非特色標籤），以與「居酒屋、串燒」一致——描述店家「是什麼」的詞屬於料理分類。

## 實作期間的修正

- **`[hidden]` 保險規則**：`style.css` 補上 `[hidden] { display: none !important; }`。原因是 `.map-legend`、`.filter-bar` 等 class 上的 `display` 會蓋掉瀏覽器內建的 `[hidden]{display:none}`，導致這些元素在該隱藏時（例如「即將新增」城市頁）仍露出。

## 開發流程

22. **受保護的 PR 流程**：不直接 commit 到 `main`，改走 Issue（限 bug／功能／重構）→ Branch（`<類型>/<說明>`）→ 實作＋preview 實測＋補文件 → PR（`Closes #n`）→ 你 review + **Squash merge**。`main` 開啟分支保護、連管理員不可繞過，並以最小 CI（`node --check` + HTML 引用檢查）為合併必要條件。細則見 [CONTRIBUTING.md](../CONTRIBUTING.md)、理由見 [ADR-0005](./adr/0005-dev-workflow.md)。

## 行程規劃

23. **多個命名行程、存 localStorage**：遊客可建立多個命名行程，收集想去的地點並分配到「第 N 天」。全部存在瀏覽器 localStorage（key `tsn.trips.v1`），重整／關分頁仍在；換裝置不同步、不做分享連結。詳見 [ADR-0006](./adr/0006-trip-planning-localstorage.md)。
24. **行程可跨城市**：一個行程就是一堆地點 id（＋各自的天數），不綁城市。第一版只有台南，跨城市暫時用不到但不擋。
25. **加入行程的入口＝詳細頁＋城市頁列表卡**：兩處放「加入行程」按鈕；首頁精選卡、地圖預覽卡不放。點按鈕彈出選單列出所有行程，可就地 toggle 加入／移除，選單底部固定「＋ 建立新行程」（建立後直接把當前地點加入）。
26. **抽象天數＋未分配**：用「第 N 天」的抽象天數桶，不綁真實日期（真實日期＝未來）。地點加入時預設落在「未分配」（`day=0`）。行程頁可加／減天數；**加天永遠加在最後，減天時該天地點退回未分配、其餘天自動連號重排**（永遠 1..N 連續、不缺號）。
27. **新增 `plan.html` ＋ `assets/js/plan.js`**：沿用 `shared.js` 與既有樣式（[ADR-0003](./adr/0003-shared-layer-per-page-scripts.md)），不引入新相依。全站導覽列加「我的行程」入口（不放行程數字標記）。
28. **行程頁地圖跟著「選中的天」走**：選第 N 天 → 地圖只框出並 auto-fit 那天的地點；沿用景點藍點／美食橘點樣式。**用「編號標記①②③＋地圖↔清單雙向連動」取代懸浮預覽卡**：圓點編號對應清單卡編號，點圓點→高亮並捲到該卡、點卡→對應圓點跳動高亮。（刻意不做城市頁那種懸浮預覽卡。）
29. **孤兒地點靜默略過**：讀取行程時若某地點 id 在 `places` 找不到（維護者下架了資料）→ 靜默略過不顯示、頁面不壞；行程頂端顯示「已略過 N 個」小提示（比照清單外標籤警告的維護者安全網精神）。

## 尚未採用（可日後再議）

- （原「git 版本控制」項已完成：專案已納入 git，並託管於 GitHub、以 GitHub Pages 部署。）
- **桌機拖曳（drag & drop）排序／搬移**：疊在同一套「移動地點到第 N 天」邏輯上，桌機專屬、手機不做；下拉選單已完整覆蓋功能，拖曳只是體驗加分。刻意延到第一版之後的獨立 PR。
- **排行程表（一天內的時間先後／時段安排）**：第一版只做到「地點落在某一天」，不排一天內順序與時間。
- **分享行程給同行者**（跨裝置共看）：需網址編碼或後端，第一版不做。
- **真實日期**（出發日＋日曆）：第一版用抽象天數，不綁日期。
- **孤兒地點的處理方式可能改**：第一版採「靜默略過＋『已略過 N 個』提示」，未來可能改為保留失效卡片、可手動清除、或搬進獨立「已失效」區等，待日後再議。
